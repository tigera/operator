apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: compliances.operator.tigera.io
spec:
  group: operator.tigera.io
  names:
    kind: Compliance
    listKind: ComplianceList
    plural: compliances
    singular: compliance
  scope: Cluster
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: Specification of the desired state for Tigera compliance reporting.
          eksConfig:
            description: 'EKS configuration to enable Compliance reporting in EKS'
            type: object
            awsSecretName:
              description: 'Name of secret resource containing AWS credentials, region information.'
              type: string
            logConfigMapName: 'Name of config-map resource containing Cloudwatch log group name, stream prefix, and fetch interval.'
              description:
              type: string
          type: object
        status:
          description: Most recently observed state for Tigera compliance reporting.
          properties:
            state:
              description: State provides user-readable status.
              type: string
          type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
