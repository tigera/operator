apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  name: tenants.operator.tigera.io
spec:
  group: operator.tigera.io
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              dataRetention:
                description: DataRetention defines the how to store a tenant's data
                items:
                  description: DataRetention defines the amount of time for tenant's
                    data to be stored based on predefined tiers
                  properties:
                    indexName:
                      description: IndexName defines the name of the index that will
                        be used to store data (this name excludes the numerical identifier
                        suffix)
                      type: string
                    policyName:
                      description: PolicyName identifies the ILM policy used store
                        data in Elastic
                      type: string
                    type:
                      enum:
                      - alerts
                      - audit
                      - bgp
                      - compliance_benchmarks
                      - compliance_reports
                      - compliance_snapshots
                      - dns
                      - flow
                      - l7
                      - runtime
                      - threat_feeds_domain_name
                      - thread_feeds_ip_set
                      - waf
                      type: string
                  required:
                  - indexName
                  - policyName
                  - type
                  type: object
                type: array
              id:
                description: ID is the unique identifier for this tenant.
                type: string
            required:
            - dataRetention
            type: object
          status:
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
